<h1>Restaurants List</h1>


<ul>
  <% @restaurants.each do |restaurant| %>
    <li><a href="<%= restaurant_path(restaurant.id) %>"><%= restaurant.name %></a></li>
  <% end %>
</ul>

<%= link_to 'Add a new restaurant', new_restaurant_path %>
